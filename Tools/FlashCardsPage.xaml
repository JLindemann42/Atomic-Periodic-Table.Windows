<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Atomic_PeriodicTable.Tables.FlashCardsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Atomic_PeriodicTable.Tables"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <!-- Element Name and Symbol -->
            <TextBlock x:Name="ElementNameText" FontSize="28" FontWeight="Bold" TextAlignment="Center"/>
            <TextBlock x:Name="ElementSymbolText" FontSize="24" FontStyle="Italic" TextAlignment="Center"/>

            <!-- Question -->
            <TextBlock x:Name="QuestionText" FontSize="20" Margin="0,20,0,10" TextAlignment="Center"/>

            <!-- Alternatives -->
            <StackPanel x:Name="AlternativesPanel" Orientation="Vertical" Spacing="10">
                <Button x:Name="AltButton1" Click="AltButton_Click"/>
                <Button x:Name="AltButton2" Click="AltButton_Click"/>
                <Button x:Name="AltButton3" Click="AltButton_Click"/>
                <Button x:Name="AltButton4" Click="AltButton_Click"/>
            </StackPanel>

            <!-- Feedback -->
            <TextBlock x:Name="FeedbackText" FontSize="18" Foreground="Green" TextAlignment="Center" Visibility="Collapsed"/>

            <!-- Next Button -->
            <Button x:Name="NextButton" Content="Next" Click="NextButton_Click" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Page>
